OPENROUTER_API_KEY=your_api_key_here

# Optional. When set, the chat function requires either this token (native app,
# sent as the X-App-Token header) or an allowed web Origin. Leave unset to keep
# the endpoint open. Must match the token compiled into the iOS app.
APP_SHARED_TOKEN=
